# Checklist — Dev (RAW) Produtos (liz_refator)

- [ ] Swagger 9004 consultado e endpoints confirmados (paths + método + parâmetros + auth).
- [ ] Existem 4 rotas dev RAW de produtos retornando `{ request, data }`.
- [ ] `/dev` tem botões RAW para as 4 rotas e renderiza `Back Request` e `Back Result`.
- [ ] TokenService permanece como está (sem refresh).
- [ ] Nenhum import direto de `lib/**` dentro de `liz_refator/**`.
- [ ] Diagnósticos TypeScript sem erros.

