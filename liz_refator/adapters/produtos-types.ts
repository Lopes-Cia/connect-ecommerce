/**
 * Adapter temporário para tipos legados.
 * Mantém o código novo dependente de `liz_refator/**` mesmo quando o tipo ainda vive em `lib/**`.
 */
export type { Brand, CategoriaNode, Produto } from '@/lib/types/produtos'

